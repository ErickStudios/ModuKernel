start() return

// funcion
Int32 fn Suma() {
    ->r3 ->r2 ->r1 
    r1+r2 
    r3<- return
}

// funcion principal
Int32 fn start() {
    // llamada a la funcion
    r1=4 r2=3 r1<- r2<- Suma() 

    // retornar
    r1=r1 return
}